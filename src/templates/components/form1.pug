section.contact
    .container
        +sectionTitle('نحن معك دائماً')
        .contact-form.mt-4.shadow
            .row.row-cols-1.row-cols-lg-2.justify-content-between(data-aos='zoom-out')
                .img-wrapper.d-none.d-lg-block
                .form-wrapper
                    .content.py-4
                        h3.text-center.color-primary.custom-title2 طلب إستشارة
                        p.text-center.color-primary.lh-base.custom-txt
                            span شكراً على اهتمامك بـ<span class=fw-bold>"بيت الخبراء الموحد"!</span> تواصل معنا وشاركنا أسئلتك وإقتراحاتك وأفكارك، فتواصلك يسرّنا دوماً
                    form.form.row.g-3
                        .col-12.col-lg-6: input.form-control.custom-txt2(type="text" placeholder="الاسم الاول" aria-label="First name")
                        .col-12.col-lg-6: input.form-control.custom-txt2(type="text" placeholder="الاسم الاخير" aria-label="Last name")
                        .col-12: input.form-control.custom-txt2(type="email" placeholder="البريد الااكتروني")
                        .col-12: input.form-control.custom-txt2(type="tel" name="phone" placeholder="رقم الهاتف" pattern="[0-9]{3}-[0-9]{2}-[0-9]{3}" required)
                        .col-12: textarea.form-control.custom-txt2(rows="5", placeholder='تفاصيل الاستشارة')
                        button.btn.mx-auto.button-primary.w-50.mt-4.submit-btn(type="submit") إرسال
